# SPDX-FileCopyrightText: 2026 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : jack2
version     : 1.9.22
release     : 2
homepage    : https://jackaudio.org/
upstreams   :
    - https://github.com/jackaudio/jack2/archive/v1.9.22.tar.gz : 1e42b9fc4ad7db7befd414d45ab2f8a159c0b30fcd6eee452be662298766a849
summary     : The JACK low-latency audio server
description : |
    JACK Audio Connection Kit (or JACK) is a professional sound server API and
    pair of daemon implementations to provide real-time, low-latency connections
    for both audio and MIDI data between applications.
license     :
    - GPL-2.0-or-later
builddeps   :
    - binary(python)
    - pkgconfig(alsa)
    - pkgconfig(dbus-1)
    - pkgconfig(eigen3)
    - pkgconfig(expat)
    - pkgconfig(opus)
    - pkgconfig(samplerate)
tuning      :
    # Clients crash if built with LTO
    - lto: false
install     : |
    for i in libjack2 libjack2-devel; do
        %install_dir %(installroot)%(docdir)/${i}
        echo "DEPRECATED" > %(installroot)%(docdir)/${i}/README
    done
packages    :
    - "lib%(name)":
        paths:
            - /usr/share/doc/libjack2/README

    - "lib%(name)-devel":
        rundeps:
            - "lib%(name)"
        paths:
            - /usr/share/doc/libjack2-devel/README
