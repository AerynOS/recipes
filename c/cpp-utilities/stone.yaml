# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : cpp-utilities
version     : "5.34.0"
release     : 9
upstreams   :
    - https://github.com/Martchus/cpp-utilities/archive/refs/tags/v5.34.0.tar.gz : 7cb228606e79b9a33244cea062674d6339b0db00081f6cae9b3b7d12619fa7db
homepage    : https://github.com/Martchus/cpp-utilities
license     : GPL-2.0-or-later
summary     : Common C++ classes and routines such as argument parser, IO and conversion utilities
description : |
    Common C++ classes and routines such as argument parser, IO and conversion utilities
builddeps   :
    - binary(doxygen)
    - cmake(Boost)
    # - pkgconfig(libarchive)
    - pkgconfig(openssl)
checkdeps   :
    - pkgconfig(cppunit)
setup       : |
    # Libarchive tests seem to fail, so don't build it for now
    %cmake \
        -DBUILD_SHARED_LIBS:BOOL=ON
build       : |
    %cmake_build
install     : |
    %cmake_install
check       : |
    # Odd, but whatever I guess
    # https://github.com/Martchus/cpp-utilities/issues/26
    %cmake_build --target check
packages    :
    - "%(name)-devel":
        paths:
            - /usr/share/c++utilities/
