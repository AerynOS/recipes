name: mandb
description: |
    Rebuild manual pages database

handlers:
    mandb:
        run: systemd-run
        args: ["--unit=mandb-trigger", "--collect", "/usr/bin/mandb", "-q"]

paths:
    "/usr/share/man/**" :
        handlers:
            - mandb
