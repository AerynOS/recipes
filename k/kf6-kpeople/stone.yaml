# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : kf6-kpeople
version     : "6.25.0"
release     : 15
upstreams   :
    - https://download.kde.org/stable/frameworks/6.25/kpeople-6.25.0.tar.xz : 8bece583572330449dfa735b589b1df276759dbbb3a1f3a4d134c5677e2bf558
homepage    : https://community.kde.org/Frameworks
license     : LGPL-2.1-only
summary     : Provides access to all contacts and aggregates them by person
description : |
    KPeople offers unified access to our contacts from different sources, grouping them by person while still exposing all the data. Furthermore, KPeople will also provide facilities to integrate the data provided in user interfaces by providing QML and Qt Widgets components.
builddeps   :
    # Temporary, boulder doesn't handle build ordering resolution correctly when builddeps are added by macros, so add ECM manually
    - cmake(ECM)
    - cmake(KF6Contacts)
    - cmake(KF6CoreAddons)
    - cmake(KF6I18n)
    - cmake(KF6ItemViews)
    - cmake(KF6WidgetsAddons)
    - cmake(Qt6CorePrivate)
    - cmake(Qt6Qml)
setup       : |
    %cmake_kf6
build       : |
    %cmake_build
install     : |
    %cmake_install
packages    :
    - "%(name)-devel":
        rundeps:
            - cmake(Qt6Widgets)
