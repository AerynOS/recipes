#
# SPDX-FileCopyrightText: 2026 AerynOS Developers
#
# SPDX-License-Identifier: MPL-2.0
#
name        : podman
version     : "5.8.0"
release     : 1
homepage    : https://github.com/containers/podman
upstreams   :
    - https://github.com/containers/podman/archive/refs/tags/v5.8.0.tar.gz : 19723cda810e087ded8903fb0f33918b10d81f7fd1d8964880c41ec30d1daa70
summary     : A tool for managing OCI containers and pods
description : |
    Podman (the POD MANager) is a tool for managing containers and images, volumes mounted into those containers, and pods made from groups of containers.
license     : Apache-2.0
builddeps   :
    - binary(go)
    - binary(man)
    - pkgconfig(gpgme)
    - pkgconfig(libseccomp)
    - pkgconfig(systemd)
rundeps     :
    - binary(crun)
    - binary(pasta)
    - conmon
    - containers-common
    - netavark
build       : |
    %make EXTRA_LDFLAGS='-compressdwarf=false -linkmode=external' PREFIX=%(prefix)
install     : |
    %make_install install.completions DESTDIR=%(installroot) PREFIX=%(prefix) LIBEXECDIR=%(libdir)

    %install_bin bin/podman
    %install_bin bin/podman-remote
    %install_bin bin/podmansh

